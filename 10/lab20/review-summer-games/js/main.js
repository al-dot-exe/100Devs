//Create a function that takes in an array of numbers. Multiply each number together and alert the product. 
function multiplyArrayNumbers(arr) {
  return arr.reduce((x, y) =>  x * y, 1)
}

multiplyArrayNumbers[1, 2, 3, 4]
